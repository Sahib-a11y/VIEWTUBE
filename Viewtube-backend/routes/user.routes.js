import express from 'express';
import authMiddleware from '../middlewares/auth.middleware.js';

const router = express.Router();

// User routes can be added here (register, login, profile, etc.)

export default router;